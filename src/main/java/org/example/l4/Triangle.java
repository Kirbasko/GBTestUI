package org.example.l4;

public class Triangle {
    public static TypeForTriangle classify(int a, int b, int c) {
        int triangle;
            if ((a <= 0) || (b <= 0) || (c <= 0)) {
                return TypeForTriangle.INVALID;
            }

        return TypeForTriangle.INVALID;
    }
}
